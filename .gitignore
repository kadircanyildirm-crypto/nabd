# Secrets — Network-as-Code credentials never enter the repository
.env
.env.*
!.env.example
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
.coverage

# Films and their build intermediates — the films are release assets, not source
*.mp4
*.webm
docs/video/build/

# Raw CAMARA call dumps — regenerable with `python -m nabd.scene --calls`
nac/evidence/nabd-calls-*.jsonl

# OS and editors
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
