# Everything Nabd needs, in two groups.
#
# The scenes, the test suites, the parity harness and the command-centre
# console need NOTHING from this file. That is deliberate: `python -m tests.run`
# and `python -m nabd.scene` are the two commands a jury will actually watch, and
# neither may depend on a wheel that fails to install on the machine in the room.

# -- the agent layer (mandatory tooling, per the Resource & Tooling Guide) ----
# LangGraph orchestrates sense → correlate → exclude → declare → triage/hold →
# brief → log. It wraps nabd/detector.py; it does not contain it.
# tests/test_nabd_scenes.py proves that by running the same scenes through both
# runners and diffing the evidence byte for byte.
langgraph>=1.0

# -- the live CAMARA backend (only for --backend live) ------------------------
-r nac/requirements.txt
